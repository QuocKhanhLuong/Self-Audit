# Active Self-Audit ACDC adapter-v2 freeze

v6 retains the v3 cohort and v2 semantic contract. It introduces a versioned semantic artifact directory, so v2 generation cannot overwrite preserved v1 semantic artifacts. No runtime workload was run.
