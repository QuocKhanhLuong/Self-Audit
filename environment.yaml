# SpecMamba / SpecUMamba — Conda environment
# Usage: conda env create -f environment.yaml
# Activate: conda activate specmamba

name: specmamba

channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.10

  # PyTorch with CUDA 12.1 (Kaggle / local GPU)
  - pytorch>=2.1
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1

  # Scientific
  # External teachers are picky: CineMA pins numpy==1.26.4 and timm==1.0.15;
  # keep these stable for CineMA + MedSAM2 cache generation.
  - numpy=1.26.4
  - scipy>=1.10
  - scikit-image>=0.21
  - scikit-learn>=1.3

  # Visualization
  - matplotlib>=3.7
  - pandas>=2.0
  - seaborn>=0.12
  - pillow>=9.5

  # Medical imaging
  - nibabel>=5.0
  - simpleitk>=2.2

  # Utilities
  - tqdm>=4.65
  - pyyaml>=6.0
  - h5py>=3.8
  - tensorboard>=2.13
  - tabulate

  # Jupyter / notebooks (e.g. kaggle_train.ipynb)
  - jupyter>=1.0
  - ipykernel>=6.0

  - pip
  - pip:
    - monai>=1.2
    - einops>=0.6
    - timm==1.0.15
    - pycocotools>=2.0.7
    - iopath>=0.1.10
    - opencv-python>=4.8.0
    - albumentations>=1.3
    - medpy>=0.4
    - wandb>=0.15.0
    # mamba-ssm requires CUDA toolkit; install manually if needed:
    # pip install mamba-ssm causal-conv1d
