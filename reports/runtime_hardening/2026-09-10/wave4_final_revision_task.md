# W4 final bounded correction (Astra REVISE)

Same repository/main worktree. You own unified_trainer.py and test_runtime_checkpoint_commit_integration.py, plus necessary narrow existing resume fixture updates and wave4.md. Other worker is released, tests/test_runtime_resume.py is frozen; root owns final reports. Preserve unrelated work. No architecture/loss/pretraining/split changes. No commits/push. Read Orca follow-ups before completion.

Root inspected final diff and reproduced two remaining bugs through the actual _trainer fixture on Python3.10/torch2.4.1:
1. last.pt epoch_history saves the current row before commit/selection fields are advanced. Saved tail checkpoint_committed=False and completed_epochs=0 while the durable report says True and1. On resume this stale row persists into the final report. Build a proposed complete checkpoint-committed row/history before saving last, including selection fields/counters. After last save succeeds adopt it in memory; preserve truthful distinction between checkpoint commit and later required JSON report commit. Do not merely patch restored history to claim missing states after the fact. Test actual saved row and resumed final row equality with baseline committed metadata, not just epoch ID list.
2. last_completed_validation is saved at payload top level (save_checkpoint merges extra) but resume reads payload['extra']; it becomes None even though saved validation exists. Restore the actual saved value and test a failure immediately after resume preserves it.

Complete previously requested strict provenance acceptance before any alias repair/output mutation: require nonempty saved/current source identity (fail closed if unavailable, not bypass); require nonempty run_id; verify last model state digest against its recorded provenance; require resumable=True, incomplete_epoch=False and validation_complete=True for exact resume. Validate epoch is within schedule, optimizer_step <= global_step and restored history tail counters/interval agree with payload and schedule. A committed best reference cannot point to an epoch later than completed_epoch. Require snapshot selection_metric, and cross-check its saved last_completed_validation[final_foreground_macro_dice] against selected best_metric (the actual observed metric, not a duplicate counter alone). Keep explicit unknown/undefined semantics.

Do not weaken negative provenance tests to obtain green. Fix stale fixtures only where the stronger contract needs generated actual checkpoint metadata. Root will run full suite once after correction; run one focused invocation for the new regression paths and report exact command/count. Fix diff --check trailing blank EOF. The fail-closed unreadable-sibling tradeoff is APPROVED by Astra, no user confirmation needed; preserve standalone best evaluation as documented.
