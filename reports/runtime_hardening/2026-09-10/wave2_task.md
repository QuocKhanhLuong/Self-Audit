# Wave 2: strict artifact IO (execute only after wave-1 reviewer gate)

Own a shared dependency-light artifact IO helper and integrations in unified trainer, calibration threshold writer, transition bank writer, audit/external CLI, validation transition cache CLI, and W&B payload conversion. Preserve all scientific metric/lineage contracts and historical compatibility tests.

- Strict recursive JSON: Tensor.detach().cpu(), NumPy scalar/array, Path, tuple/list, None, finite primitives and nested mappings. Reject custom objects and key collisions with a useful tree path; no default=str fallback for arbitrary objects. Handle array elements recursively. Nonfinite numeric metrics become null, never zero, with explicit documented serialization policy; use existing valid/defined metadata and add policy metadata only where the schema permits it. Do not break strict calibration headers or frozen-bank content signatures. Empty and undefined metrics stay distinguishable from numeric zero.
- Atomic same-directory temporary write, flush + fsync + os.replace, best-effort cleanup without masking primary exception. Fail before replacing an existing file on serialization/fsync/replace errors. Do not swallow ENOSPC. Reuse wave-1 primitive where suitable. Preserve previous valid artifact.
- Every active pipeline_report/calibration/diagnostics/audit/external/bank final write goes through shared helper. Validation transition caches go through safe atomic torch writer. Report schema/version explicit. Preserve semantic distinctions among validated, calibration-only, held-out and oracle diagnostics.
- W&B config/log/summary nested conversion must not silently emit NaN/Infinity or discard arrays/null; telemetry failures stay observable, not silently successful. Lifecycle finish(exit_code) and per-epoch commit ordering belong to wave 3, coordinate APIs accordingly.
- Regression tests exercise real writer entrypoints, not just helper mirrors: nested nonfinites, unknown custom object, schema roundtrip, valid old-file preservation, serialization/flush/fsync/replace errors, external report with undefined Dice, no calibration/bank lineage drift. Run new tests and affected existing calibration/checkpoint/bank/external tests on actual torch2.4.1 Python3.10.
- No recipe changes, no commits/push, preserve unrelated work. Return report with exact commands/results and limitations.

Additional boundary found during exhaustive literal scan: scripts/calibrate_threshold.py:_load uses weights_only=False then an implicit unsafe fallback. Update the active transition-cache CLI loader to explicit weights_only=True on declared torch2.4.1, with a clear unsupported-legacy-cache error and regression that arbitrary/custom objects are rejected. Do not add automatic unsafe pickle fallback.

Include legacy pipeline shared _write_json helper (scripts/train_self_audit_legacy.py:211) because canonical CLI reexports legacy report/calibration helpers for reproducibility. Data-preparation-only manifests in preprocess_*/acdc_split/prepare_mnm_binary are outside active runtime and can remain unchanged; list that scope explicitly. Do not touch binary M&Ms preparation behavior.

Carry forward minor W1 cleanup: remove unreachable bytes/bytearray return branches in training normalizer and stale docstrings claiming byte normalization; bytes are intentionally rejected for2.4. Remove unused imports when touching the helper. Functional W1 is root-PASS (94 independent tests), but final shared-helper docs should be accurate.
