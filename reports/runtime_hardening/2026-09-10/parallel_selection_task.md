# Claude: isolated immutable selection helper for W4

Repo /Users/alvinluong/Self-Audit. You are not alone. Own ONLY new src/self_audit/training/checkpoint_commit.py, new tests/test_checkpoint_commit.py, and report parallel_selection.md. Do not edit unified_trainer.py or _utils.py; main AGY integrates your agreed API. No architecture/loss/config/recipe edits, no commits/push. Read audit_plan R9 and wave4_task.md for context. Python /private/tmp/self-audit-torch241/bin/python; RTK prefix, PYTHONPATH=src:., single-thread test.

Implement dependency-light filesystem helpers for immutable selected best + last commit protocol. Main trainer will save a candidate checkpoint at a UNIQUE path selected_best/epoch_<epoch>_<uuid>.pt using existing save_checkpoint, put a reference into last.pt BEFORE updating public best.pt, then call your atomic alias publisher. If saving last fails, old last references an untouched snapshot. If alias publication fails after last commits, next resume resolves the immutable reference and repairs public alias. No in-memory-only rollback design.

Agree/provide these APIs (send exact signature to root early):
- make_best_reference(snapshot_path, checkpoint_dir, *, epoch:int, metric:float) -> dict with schema_version=1, path relative to checkpoint_dir, sha256, epoch, metric. Require existing regular file, within selected_best directory, finite metric and positive epoch; do not permit path escape. Do not alter checkpoint bytes.
- resolve_best_reference(reference, checkpoint_dir) -> Path: strict reference field/type/path/hash validation. Hash verified before consumer loads. Main validates checkpoint payload run/config/state/selection metadata after safe load.
- publish_best_alias(reference, checkpoint_dir) -> Path: resolve verified immutable source; copy bytes to same-dir temporary, flush+fsync, os.replace best.pt, best-effort directory fsync, cleanup without masking. Old best preserved on serialize/read/fsync/replace error; no unsafe symlink-following final target.
- relocate_best_reference(reference, source_dir, target_dir) -> dict: copy the exact verified snapshot to same relative path atomically; refuse conflicting existing bytes/unrelated file; preserve source. Alias publication remains explicit separate call.

Helpers must never import _utils or unified_trainer (avoid circular dependency); use stdlib and existing dependency-light primitives only where appropriate. Do not create a second torch serialization policy. Validate source bytes identity across copy and fail closed if changed. Do not garbage collect old snapshots. Tests: expected SHA preserved; failing fsync/replace leaves old alias valid/no temp; malformed/path traversal/hash corruption rejected; relocation refuses conflict; interrupted alias followed by explicit recovery succeeds. Small byte fixtures are enough for filesystem logic. Full actual model pairing/resume is main AGY work. Deliver one focused test invocation and report.
