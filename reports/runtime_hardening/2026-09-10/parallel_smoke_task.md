Repo /Users/alvinluong/Self-Audit, base main 25f429a46255ceb59997ae5178939d5d40b2ea27. User now explicitly requests parallel AGY and Claude and fewer repeated tests. You are NOT alone; preserve others edits and obey exact file ownership. No commits/push, no architecture/loss/pretraining/gate/split changes, no long training. Root Astra reviews and integrates. All shell commands start rtk (read /Users/alvinluong/.codex/RTK.md). Semble before semantic discovery; no .codegraph. Compatibility Python is /private/tmp/self-audit-torch241/bin/python (Python3.10 torch2.4.1 CPU); use PYTHONPATH=src:. PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1. Default python torch2.13. Read audit_plan.md and relevant current source; Waves1/2 are reviewed, Wave3 central trainer lifecycle is being edited by another AGY. Use only focused tests once after your edits; full suite belongs to root at final integration. Read coordinator inbox at checkpoints and before worker_done, never expose lifecycle capability.
OWNERSHIP: new tests/test_runtime_pipeline_smoke.py and reports/runtime_hardening/2026-09-10/parallel_smoke.md only (optional new helper under tests with runtime_smoke name). No production files or existing tests. Implement bounded real subprocess CLI integration: short 3-interval tiny synthetic ACDC fixture with full validation/calibration/diagnostics, canonical unified config overridden only for fixture/model cost, best.pt/last.pt and validation cache. Then standalone scripts/export_transition_bank.py and scripts/evaluate_external_mnms.py on separate prepared synthetic M&Ms held-out cases using frozen ACDC best and fixed calibrated tau. Read real CLI flags, factory and lineage; do not mock dataset/binding/evaluator in success smoke. Assert strict JSON, completion/counters, selected checkpoint hash, calibration identity, external labels cannot tune anything, class mapping/shape preserved. Native MNMS separate tiny config smoke if practical. Add subprocess timeout and inherited env, cost bounded. Trainer is concurrently changing, so prepare tests and execute targeted smoke once; report concrete blockers to root without editing production. Do NOT introduce automatic bank export. Do not run full suite.
