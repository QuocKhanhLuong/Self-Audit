# AGY independent exact-resume regressions for W4

Repo /Users/alvinluong/Self-Audit. You are not alone. Own ONLY new tests/test_runtime_resume.py and report parallel_resume.md. Also correct your parallel_portability.md factual sentence: bind leaves model on EXISTING model device; it does not move CPU model to requested map_location. No production edits, no changes to existing tests or other reports. Main AGY owns W3/W4 trainer; Claude owns checkpoint_commit helper and W&B wrapper. All shell commands start rtk, Semble before semantic discovery. Python3.10 torch2.4.1 /private/tmp/self-audit-torch241/bin/python, PYTHONPATH=src:. OMP_NUM_THREADS=1 MKL_NUM_THREADS=1. No full-suite repetition, commits or push.

Prepare a compact real-training regression comparing uninterrupted vs resumed model digest, optimizer/scheduler/scaler/RNG/counters across a 6-epoch tiny curriculum (2 bootstrap,2 auditor,2 joint), capturing legitimate completed epoch checkpoints at positions1,2,3,4,5. Use one baseline fixture, parameterized resumes of its suffix; no fabricated counter edits and no config extension on resume. Capture last.pt after actual atomic writes or copy saved legitimate states, with immutable selected-best references retained. Ensure varied batch sizes/loader cardinalities, freeze masks, audit weights, rollout mode and LR do not restart incorrectly. Keep fixtures small image32/few slices. For these unit resume tests only, forcing dependency-light fallback encoder through monkeypatch during model construction is acceptable to limit snapshot size, never change canonical pretraining; actual unpatched ConvNeXt CLI is covered by separate smoke worker. Do not use max_steps-bounded nonresumable checkpoints as if resumable.

At least one test must exercise actual shuffled augmented nonpersistent num_workers=2 DataLoader continuation or explicitly report missing coverage if platform spawn prevents it. Investigate whether dedicated DataLoader/sampler generator state is saved; if not, send confirmed divergence to root early. Existing source may still be pre-W4: report failing exact-resume premises without weakening assertion or mocking away RNG. Main AGY will fix implementation. Include final calibration after a resumed completed curriculum and preserved epoch history, using real postprocessing where feasible. You can report tests ready with known failures until W4 integration; do not invent shims. User requested faster tests, so use one targeted invocation for evidence rather than broad retries. Before worker_done read inbox with CURRENT preamble capability; never recover tokens from private stores.
