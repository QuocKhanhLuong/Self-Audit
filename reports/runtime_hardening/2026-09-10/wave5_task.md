# Wave 5: integrated runtime verification

Implement bounded end-to-end synthetic subprocess tests and a short documented GPU canary procedure. No long training or scientific claims. Test defaults derive from canonical config; tiny fixture overrides explicitly disable pretraining/fallback only for tests. Use real paired 32x32x2 ACDC volumes and patient-disjoint manifest; external M&Ms uses a separate testing directory and explicit four-class mapping. Execute full short curriculum, save selected best/last, calibration, diagnostics, bank and cache, then frozen external evaluation with fixed tau. Strict JSON parse rejects parse constants; verify report/checkpoint binding, hashes and absence of external training/calibration.

Compare console, pipeline report, checkpoint epoch metadata and mocked W&B for counters, interval, schedule weights, post-step LR semantics, measured initial/candidate/final Dice. Check failure subprocess nonzero with truthful artifact, and no completed-epoch log before commit. Resume after completed epochs in each interval/boundary; verify calibration after resumed completion and public best!=last provenance. At least one test exercises current canonical native M&Ms configuration runtime/report path without mixing experiment identities.

Audit GPU-only test branches too: initial wave1 test_cuda_rng_state_handling expected text 'does not match available devices' but implementation returned different wording, so this test would fail on a GPU despite CPU PASS. Fix coverage with mocked CUDA count/state APIs plus optional real CUDA branch. GradScaler roundtrip must include nonempty enabled state (torch.amp.GradScaler('cpu', enabled=True) is available on2.4.1); disabled {} state alone is insufficient. CPU tests do not certify CUDA RNG kernel execution or bf16 GPU memory.

Run full repository tests with actual `/private/tmp/self-audit-torch241/bin/python` (Python3.10, torch2.4.1, torchvision0.19.1, NumPy1.26.4) and local Python (torch2.13), saving exact JUnit counts. If an optional MPS fixture fails, distinguish unsupported local backend from CUDA target; make fixtures explicitly CPU when appropriate without hiding a target-runtime failure. Keep all negative provenance tests. Report exact observed commands/counts, no invented aggregate counts. Root independently reruns final suite and subprocess smoke before approving logical commits/push.

Clarification from source audit: UnifiedTrainer ends postprocessing at final_diagnostics_complete. Frozen transition bank export is the standalone scripts/export_transition_bank.py CLI. Smoke that CLI explicitly after checkpoint selection; do not add automatic bank export to trainer or invent a config option. Root wave2_independent_probe.py provides real model/binding/valid-bank IO evidence; permanent W5 tests must exercise full actual subprocess paths and can reuse that fixture approach.
