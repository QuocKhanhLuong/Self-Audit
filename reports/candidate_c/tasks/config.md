# Worker 5 — config and baseline interfaces

Read current HEAD expected 9a612485b7ead1273082d7dd52879f8e9e7a3e83 and reports/candidate_c/architecture_contract.md. Own only src/self_audit/training/unified_config.py, training/_utils.py, configs/self_audit_full.yaml, tests/test_candidate_c_config.py, reports/candidate_c/baseline_interfaces.md. You are not alone; never revert others, no commit/push/spawn. No core/training trainer/M&Ms config edits. Coordinate their necessary changes through coordinator.

Implement strict config/model builder switches model.window_mode current(default), feature_only, free_offsets, candidate_c, candidate_c_no_fix, direct_rollback, with candidate_c settings specified by contract. Core worker owns CandidateCConfig and SelfAuditNet constructor, accepts validated mapping. Ensure model option not silently filtered by filter_model_config. Legacy config and canonical serialization roundtrip must retain all knobs and strict unknown/nonfinite/invalid-field errors. Old configs still load. Keep baseline defaults/A0/pretrained ConvNeXt and 130-epoch recipe exactly. Do not duplicate models or run experiments. Add optional training.rollout.predicted_history_exposure=false/predicted_history_weight=0.1 strict fields, trainer worker consumes these. Update canonical ACDC YAML with documented default switches; M&Ms worker owns native YAML and receives matching schema contract.

Dataset builder changes requested by M&Ms worker may be routed here because you own _utils.py; coordinate via coordinator. Do not add speculative broad changes. Check checkpoint/provenance binding and resolved model config must include execution mode; never allow external ACDC->M&Ms solver tuning or silently differing config. Let runtime reviewer identify wider provenance edits if any.

Tests 1-3 focused files/<2min CPU plus compile/import using /private/tmp/self-audit-torch241/bin/python if valid. Test each switch reaches constructor (wait for core or isolate mock), old config defaults, strict validation, loss exposure fields and resolved roundtrip. All shell commands prefix rtk, Semble for code discovery. Deliver report with exact CLI/config examples, owned changes/test evidence and limits. Follow injected Orca lifecycle.
