# M&Ms review — REVISE, reject heuristic protocol enforcement

Coordinator inspected actual diff and new utility. You retain existing ownership; add tests/test_candidate_c_pipeline_smoke.py if needed for a bounded native CLI fixture. No core/config-schema/trainer edits, no commit/push, you are not alone, preserve others. Complete these corrections before claiming integration PASS:

1. REMOVE both duplicated `verify_no_cross_dataset_leakage` path/name heuristics and their call. A single M&Ms record under `/tmp/test_acdc_to_mnms/mnm` or with patient ID `patient001` currently triggers a false contamination failure. Paths and the word patient are not authoritative dataset provenance. Existing explicit dataset selection, patient split validation and checkpoint training-dataset metadata are the enforcement points. Delete NEW dataset_resolution.py if it is only standalone uncalled helpers/test wrappers; do not add disconnected protocol/availability framework and claim it enforces the actual runtime. Remove mirrored tests of those helpers; keep real entrypoint/provenance negative tests. Add a regression loading legitimate M&Ms under a parent path containing both acdc and mnms.
2. Keep existing MNMSDataset depth_axis=None semantics unless you demonstrate an actual bug in the canonical explicit depth_axis=2 flow. Do not globally override unspecified geometry as a speculative fix. Verify explicit H,W,Z axis2, paired image/mask, labels and patient splits through real existing data factory.
3. Remove opportunistic numeric-string coercion in external solver settings. Shared strict validator is authoritative; JSON numeric values remain numeric, invalid strings fail. Update tests/config fixtures to use unambiguous numeric YAML/JSON. Remove broad except Exception around additional checkpoint load. Prefer reuse existing safely inspected payload or explicit fail-closed load; no silent empty-config fallback after a load failure.
4. External mode/solver configuration must remain frozen to its ACDC checkpoint/evaluation identity. No silent overriding an explicitly conflicting config with checkpoint mode, and no CLI override that bypasses source checkpoint policy/settings binding. If --window-mode is kept, validate exact choices and reject conflicts; source settings fallback only for genuinely absent config fields. Explicit mismatch in candidate_c settings must fail. Verify runtime worker centralized provenance; do not invent duplicate permissive binders. serialize ACTUAL CandidateCConfig as plain dict, not raw dataclass/string or wrong attribute fallback. Source checkpoint metadata/declared config must be checked, not only the config used to construct the new model.
5. The report currently overclaims native unified Candidate C execution: tests construct datasets/models and one external subprocess; they do not run native C training CLI. Add one bounded native M&Ms unified synthetic subprocess smoke using config self_audit_full_mnms.yaml with Candidate C selected, tiny local fixture, few steps/batches, no pretrained download in TEST ONLY. Reuse tests/test_runtime_pipeline_smoke.py helpers where possible, no giant copied suite. Prove actual optimizer/checkpoint/report path and state measured; distinguish solver eligibility from merely C mode selection. Coordinator later runs combined final tests. Keep normal <2min budget; if expensive send exact reason and finish other fixes.
6. Revise mnms_integration.md to match actual guarantees and tests; no invented prefix-overlap rejection/authoritative helper claims. Document exact native ACDC, native M&Ms, frozen external commands using scripts/train_self_audit.py, no legacy phase_c_best naming as canonical best. Add optional exposure fields to native YAML matching canonical default false/0.1 (no schedule change).

Report actual evidence and remaining constraints. All shell commands rtk; no long training. Follow live lifecycle then idle.
