# Training review — REVISE two correctness/measurement details

Coordinator inspected both production diffs. Preserve current ownership and others' changes, no commit/push. Primary objective preservation, official gate, detach boundaries, invalidation and scalar-only reporting look consistent. Required bounded revisions:

1. `compute_predicted_history_audit_loss` re-encodes the image after `collect_predicted_history_transitions`, although infer already returns shared_features from the exact collected trajectory. Reuse that detached shared tensor for the auditor loss; no extra encoder forward and no fallback to raw images pretending to be shared features. Keep helper interfaces internal as needed, avoid cloning the full feature once per turn. Test encoder call count one for this auxiliary path and exact same collected features supplied to auditor. Explicitly document official Auditor collection passes plus gradient-bearing auditor loss passes as added cost.
2. `accepted_history_count` implementation counts ALL accepted transitions, including C/rollback, but docstring says ordinary. Correct it. `real_evidence_attempts` counts attempts after previous acceptance, which for C is latest evidence consumed by solver, not necessarily by the generator during a trainable ordinary annotation. In particular first restitution replays the previous update whose audit input may be None and detaches its innovation. Do not call that a trainable real-audit generator exposure. Keep agreed six counters, add a clearly named ordinary-annotation real-evidence attempt count (use per-sample accepted_path ordinary where available; for baseline ordinary infer all post-accept attempts qualify). State this counts forward exposure, not guaranteed parameter gradient; accepted conditional updates/gradient probes would be separate. Add a small fixture covering ordinary -> restitution -> ordinary with corresponding counts.

Keep opt-in default, stages, gradient firewall and invalidation unchanged. Focused training test plus compile sufficient; don't rerun broad regressions again unless failure warrants. Update training_integration.md and settle.
