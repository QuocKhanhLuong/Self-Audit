# Diagnostics final actual-diff review — REVISE solver-work totals
Root reviewed revised module/script. GT proposal-vs-retained/history admission and per-batch geometry reduction PASS pending final integration. You retain exclusive ownership and must preserve others; no commits/push.
One material compute bug remains: summarize_diagnostic_rows sums evals over all sample rows although core emits the SAME batched solver invocation counts to each row. With B4 group and2 survivors, total_forward3 is currently reported6 and CLI claims actual work. Dividing by solver_group_size4 would be wrong too because halted rows have no emitted row. Deduplicate actual invocation counts by scoped batch id + turn + record_turn (core currently runs at most one solver invocation per turn). Prefer explicit solver_invocation_id if core adds it; ask core via dispatch ctx_d00b135edd42 only for coordination. probe_candidate_c_path must namespace repeated sample/turn IDs per batch. Report sample-associated means separately from actual invocation totals and per-row opportunity counts, do not silently rebrand raw sums. Test B>1 partial survivor and repeated batch IDs; group inconsistent counts must report unknown/error not fabricate. Final raw total_forward/factual_replay/checks sum keys and CLI must use actual invocation totals, explicit excludes official Auditors/ordinary annotator work. Keep zero-forward baseline guard.
Core is correcting C1 failure to NORMAL annotation instead of identity/crash, direct runtime config hard bound<=2, genuine half projected support backtracking, and separates feasible from improved + measured failed-check diagnostics. Keep readers compatible, additions okay. Your history-based GT admission should already include such normal fallback rows with record_turn. Do not claim all code is stable until new core settles.
One short focused test file and compile sufficient. Update report, settle.
