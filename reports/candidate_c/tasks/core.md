# Worker 1 — Candidate C core, requested Claude Opus 5

Implement user-selected Candidate C, never Candidate B, in Self-Audit main base 9a612485b7ead1273082d7dd52879f8e9e7a3e83. First inspect current HEAD and owned files. Read reports/dynamic_window_research/candidate_C.md and reports/candidate_c/tasks/architect.md. Astra is writing reports/candidate_c/architecture_contract.md. You may inspect and plan NOW, but before production edits read that completed contract or ask coordinator for readiness; send interface questions early. Do not spawn workers. You are not alone: never revert others, edit only owned files, no commit/push.

OWNERSHIP: src/self_audit/models/dynamic_window.py, annotation_expert.py, self_audit_net.py; tests/test_candidate_c.py; reports/candidate_c/opus_core_report.md. Other workers own training, config/schema/builders, diagnostics and data. Coordinate API names through coordinator. Do not edit their files.

Required: coordinate override preserving Q/K/V behavior, all per-internal-iteration geometry with turn/index/coords/preclamp/attention/state identity. Bounded record only latest accepted transition: prelogits, stable shared features, prior audit input, original turn/all indices, factual supports/candidate/gate, model state identity. C1 exact frozen factual-support replay must pass strict numerical tolerance BEFORE coordinate optimization; otherwise log and use ordinary path. Alternative Q changes ONLY realized sampling coordinates at same recorded state. Freeze parameters/input/auditor and allow gradients only Q during solver. Must work in caller no_grad/inference_mode, and restore parameter flags/grad state without leakage. Handle optimizer invalidation. Infer also serves joint training; agree gradient policy with Astra/training worker.

C2 predicted REGRESS evidence drives weighted CE to pre-transition argmax + lambda mean square displacement. Use rho about one feature pixel, axis-correct normalized coordinate conversion and bounded domain. Protect every predicted FIX pixel above threshold with factual winning class margin (fraction positive factual margin, log ties). One projected proposal and at most TWO backtracking candidate checks; no unbounded optimizer. Empty/negligible R, infeasible/nonimproving/nonfinite => factual support. C3 current + spatial class-shared bounded recorded gate*(B_cf-B_factual). Exact subtraction essential. Official detached Auditor still judges final candidate with delta_q>tau and reject->HALT. No GT in runtime. Preserve A0/ConvNeXt, current window baseline, checkpoint/calibration provenance.

CRITICAL: a C-produced accepted transition is not automatically replayable as an ordinary updater from its immediate pre-state. Follow Astra bounded record-kind eligibility contract; do not relabel an older ordinary action as most recent accepted. No recursive unbounded records. Record eval counts including factual replay, coordinate backward, candidate checks; retain per-case active batch correspondence.

Expose core switches per Astra API for current, feature-only (both explicit audit and input projection audit disabled), free offsets, C, C-without-FIX, direct rollback without duplicating model. Config worker will wire schema/builders; do not touch config files.

Run only focused test_candidate_c and up to two existing focused files, compile/import, tiny synthetic forward/backward; normal CPU budget <2min. Existing /private/tmp/self-audit-torch241/bin/python may be used after verifying. All shell commands prefix rtk. Semble for code discovery; no .codegraph. Tests must actually demonstrate replay identity, multi-iteration override, successful feasible solver (not just identity fallback), margin preservation, bounded evals, fallback, innovation subtraction, detached gradients and auditor authority. Report C1/C2/C3 mapping, changed files, tests actually run, replay/fallback/compute limitations. Follow live Orca preamble and worker_done exactly once.
