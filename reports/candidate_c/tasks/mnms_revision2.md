# Final M&Ms actual-diff review — narrow frozen-mechanism ambiguities
Your 6 corrections are visible and mostly good. This is a NEW follow-up dispatch: use the task/dispatch IDs and capability in its authoritative preamble. The prior six-item revision is completed; do not settle it again. Ownership unchanged; no trainer/model edits, no commit/push, preserve otherworkers. Additional permission: you may update tests/test_external_mnms_evaluator.py only to make stub model metadata truthful if needed.
Required:
1. Remove the new `if hasattr(model,"num_classes")` bypass around verify_model_config. Runtime cannot skip source-config verification because a model lacks a declared field. If old teststubs lack metadata, improve fixture metadata/verification contract; don't weaken production checks for tests.
2. Source mode/settings resolution currently uses elif precedence and silently ignores conflicting declarations (checkpoint.config.model vs checkpoint.config flattened vs checkpoint top-level; evaluation model vs top-level). Collect all actual declared mode/settings occurrences and reject disagreement. Also compare recorded runtime model identity under checkpoint.provenance.model_identity (and canonical top-level ifpresent) when it declares window_mode/candidate_c_settings, so a checkpointcannot claim two different execution mechanisms. Use existing strict validators; don't expand provenance frameworks. The real canonical config+provenance should agree.
3. Explicit candidate_c:null is not absence; shared validator defines null as default settings. If evaluation config explicitly null while checkpoint customrho2, reject the mismatch instead of silently inheriting2. Only absent keys inherit source. Similarly explicit invalid/null window_mode is rejected; no silentdefault under malformedfields.
4. Legacy checkpoints with missing mechanism settings may keep current baseline compatibility, but may NOT be silently certified as a frozen nondefault CandidateC mechanism merely from CLI/evaluation overrides. Fail closed for nondefault runtime when source mechanism identity cannotbeestablished. Source-checkpoint config contains allnewfields in canonicaltraining so supportedflowunaffected. Report legacybaseline vsfrozenC requirementexplicitly.
5. The new nativeCLI test only exercises bootstrap, so state that narrowly: it does not execute full3intervalC curriculum or demonstrate an eligible solve. Root will separately run existing allintervalruntimepipeline plus syntheticCjoint tests. If inexpensive, prefer one shortstepperintervalnativeC smoke usingexistingruntimehelpers so fullnativeflowtested, but no extra longrun and no false best.pt claim whenonlylastwritten. Correct docstring/report exactlyactualsmoke.
Focused M&Ms unit+pipeline+existingexternaltest (max3files) withthreads1; capture actualstdoutsummary. Source may move while root's strictFIXworker finishes; source-binding failuresduringconcurrentedits are provisional, no repeatedlongruns. Settle correctly and reportactualtestnames.
