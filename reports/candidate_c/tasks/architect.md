# Astra XHigh architecture contract and final-review ownership

You are the lead architect/planner/final reviewer for user-selected Candidate C, through Orca. This first dispatch is PLANNING ONLY. Inspect actual latest HEAD and source before planning. Base currently verified origin/main=9a612485b7ead1273082d7dd52879f8e9e7a3e83. Do not implement production code, commit, push, or spawn workers yourself; coordinator dispatches your decomposition. You are not alone in this checkout; preserve unrelated untracked work and never revert others' edits.

Read reports/dynamic_window_research/candidate_C.md and current_mechanism.md, conditioning_mismatch.md. User now explicitly authorizes implementing C, superseding their research-only wording. Do not switch to Candidate B. Read docs.md as applicable, use Semble for discovery (CLI fallback uvx --from 'semble[mcp]==0.5.5' semble); no .codegraph exists. All shell commands prefix rtk. Existing /private/tmp/self-audit-torch241/bin/python may support focused tests. This is architecture planning, no tests required.

Contract: record only most recent accepted transition, complete factual realized coordinates for every internal iteration and fixed prior inputs/turn/indices/gate/state identity. C1 U_frozen(record;P) must strictly reproduce factual candidate before optimization; failure disables C for transition, logs reason, falls through ordinary annotator. C2 uses predicted REGRESS CE restoration to pre-transition argmax class plus lambda mean squared coordinate displacement; trust region rho=1 feature pixel around realized P and spatial bounds; predicted FIX target is factual argmax with positive margin, protect all thresholded FIX locations. One projected gradient proposal plus at most TWO backtracking candidate checks; no meaningful REGRESS / infeasible / nonimprovement => factual support. C3 current + class-shared spatial g*(B_cf-B_fact). Official detached Auditor still independently evaluates and strict delta_q>tau accept, rejection HALTs. No inference GT, no change A0/pretrained ConvNeXt, provenance/calibration/unified best/last guarantees.

Resolve critical design choices explicitly:
1. Accepted C-derived transitions: their factual output is not an ordinary AnnotationExpert update from their immediate pre-state. How retain bounded replay for only most recent accepted transition WITHOUT pretending stale ordinary record represents it? Choose an honest minimal eligibility/record-kind contract; document if next turn returns to ordinary path after C acceptance rather than recursively nesting unbounded records.
2. Exact replay modes, dtype, cloned detached tensors, model parameter versions and state identity. Existing infer is used for joint gradient training and has no no_grad decorator. Deployment may wrap inference_mode. Coordinate-only gradient region must work under inference_mode/no_grad without changing parameter gradients/flags or leaking to encoder/Auditor/input.
3. Minimal opt-in matched predicted-history curriculum; preserve 130-epoch baseline schedule. No stale replay across optimizer steps. Clarify whether solved C innovations are detached during training and how meaningful annotator gradients remain. Do not hide extra evaluations. Avoid full redesign.
4. Config schemas + model builders have strict parsing. Allocate nonoverlapping ownership and freeze concrete API names/types for workers. Baseline switches current, feature_only (remove BOTH audit entry paths), free_offsets, candidate_c, candidate_c_no_fix, direct_rollback; zero new learned parameters preferred.
5. Diagnostics schema and per-case active batch bookkeeping, per-turn capture, factual identity tolerance; solver eval budget including identity replay and backwards. Guard nonfinite values/ties/zero evidence/trust region axis conversion/1x1 features.

Worker allocation suggested: core owns models/{dynamic_window,annotation_expert,self_audit_net}.py and tests/test_candidate_c.py; training owns training/{unified_trainer,finetune_joint}.py and tests/test_candidate_c_training.py; M&Ms owns data/mnms.py, scripts/evaluate_external_mnms.py, configs/self_audit_full_mnms.yaml, dedicated M&Ms tests and new dataset resolution utility if needed; baseline owns config schema/builders (locate concrete files) and configs/self_audit_full.yaml plus dedicated baseline tests, NOT core; diagnostics owns scripts/audit_checkpoint.py and specific new evaluation module + dedicated tests; runtime review read-only; red team read-only after integration. Identify any shared builder/entrypoint edits before assigning.

Deliver reports/candidate_c/architecture_contract.md with exact HEAD, API contracts, ownership map, temporal state machine, C1-C3, acceptance invariant, compute budget and final-review checklist. Send coordinator a concise architecture-ready message promptly when interfaces are decided, then finish full report. Later dispatch will ask you to inspect each actual worker diff and independently issue PASS/REVISE/REJECT and final GREEN only after required gates. Do not declare GREEN in this planning pass. Follow injected Orca lifecycle exactly.
