# Independent Candidate C read-only red team
You are a Claude worker under the sole Astra root coordinator through Orca. You are NOT a planner and must not spawn other agents. Read actual latest combined diff, not worker claims. Repository /Users/alvinluong/Self-Audit base main 9a612485b7ead1273082d7dd52879f8e9e7a3e83. All workers have now settled before this dispatch. Your only write ownership: reports/candidate_c/red_team.md. No production/test edits, no commit/push. You are not alone: root will run selected final gates while you review; preserve all files. All shell commands rtk. Semble discovery where needed; known core files can be read directly; no .codegraph directory.
Read architecture_contract.md and latest core/training/diagnostic/M&Ms/provenance actual diffs. User chose C, not B. Independently try to falsify:
- C1 exact original accepted ORDINARY correction replay, same prestate/features/audit input/turn/all depths/autocast/factual batch composition; changed only realized support. C1/stale identity failure MUST use ordinary normal current-state path and official audit, not crash/identity masquerade.
- C2 only coordinate gradients, full model/image/auditor frozen via functional detached params; preserves pre-existing grad/state; one gradient proposal, <=2 TOTAL checks even direct runtime config. Genuine half-step backtracking after projection, one-feature-pixel bounds non-square and size1 axes, predicted FIX class margins. The last strict-FIX patch protects ALL thresholded FIX pixels, including ties and tiny margins, with explicit argmax checks on Bcf and final transfer. Try to break it. No GT runtime, no stale generation/weight/retained-state records.
- C3 current+class-shared gate*(Bcf-Bfactual), exact0 identity fallback, final FIX checks, Auditor detached strictdeltaQ>tau/rejectHALT. No nested replay mislabelled as ordinary. Mixed sample indexing/replay accepted flags correct.
- Runtime default current bitwise unchanged, A0/pretrained same; feature-only removes both explicit E paths; free offsets not restricted to weaker0.12 support reach.
- Training opt-in flag unchanged130epoch primary objectives; zero/predicted evidence counters count correct CURRENT path and failures; no GT into gate; records local only and invalidated after steps. Auxiliary cost and limits honest.
- Diagnostics include rejected/identity/ordinary-fallback historicaltriplets, proposal vsretained, unknown notzero, actual batch-deduplicated solver counts, no raw coordinate tensors acrosscohort/checkpoints.
- M&Ms 4class rawmapping0:0,1:3,2:2,3:1 correctaxis explicit2 and defaultNone, patient split/paired shape/unknownlabel fail, no path-substring contaminationheuristic. Native flows distinct, external frozen ACDC metadata/mode/settings/calibration no M&Ms tuning; best/last atomicresume/provenance signed mechanism and runtime ephemeral.
Give PASS/REVISE/REJECT with file:line and concrete executable counterexample for material bugs. Tiny isolated probes allowed under2min CPU with /private/tmp/self-audit-torch241/bin/python, OMP/MKLthreads1; root handles tests, don't rerun large suites. Distinguish blocked GPU (4070 SSH auth denied and no local data) from codefailure; never claim GPU/medical/novelty/effectivenessPASS. Report code approval separately from unmetexperiment/pushgates. Name unresolved risk if not repro, not definitivebug. Settle worker_done with report and findings.
