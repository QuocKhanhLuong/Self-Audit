# Training actual revision review — REVISE temporal counter semantics
Root inspected revised code. Feature reuse correction PASS. Two counters still scientifically wrong and must fix, ownership unchanged, no commit/push, not alone.
1. ordinary_annotation_real_evidence_attempts currently tests previous_ordinary at t-1. This is reversed: previous ordinary -> current C consumes evidence in SOLVER, while previous C -> current ordinary consumes evidence in trainable GENERATOR. Count previous accepted state_masks[t-1] AND active_masks[t] AND CURRENT row accepted_path == ordinary, regardless of current acceptance (forward exposure). Baseline all ordinary. Rename helper to _ordinary_attempt_masks and align to active masks/current turn. Your 3turn 2sample fixture had equal totals despite wrong temporal rows, so add asymmetric 2turn fixture ordinary->C expects ordinary_evidence0 (real2); 3turn ordinary->C->ordinary expects perturn0,0,2; C1failure routed ordinary consumes actual previous predicted evidence and counts. Correct docs explaining that evidence source type does not determine receiving path.
2. candidate_c_attempts currently filters eligible True, so failed C1 (eligible False) and no REGRESS mass never contribute; candidate_c_replay_failure can NEVER increment for real core failures. Count actual record-consuming C attempt by valid record_turn + record_kind ordinary (or core explicit attempted field if agreed), regardless of solve eligibility. Include replay_failure/stale failure and no-regress fallback; fresh ordinary with no record is not attempt. Feasible/failed/fallback counters within that actual-attempt denominator. Core is currently revising numeric/state C1 failure to normal annotation with same logged origin record_turn, so that still counts as attempted C plus ordinary exposure. Coordinate with core if necessary, do not await architecture request.
3. Report exact auxiliary cost T annotator forwards only applies current baseline. Candidate C adds 1 differentiable historical replay and up to2 candidate checks per record group plus coordinate backward; original factual batch can exceed active count. Call it out, no hidden double count or claim same T cost.
Only focused tests and compile, threads1. Correct real fixture values and report, then settle.
