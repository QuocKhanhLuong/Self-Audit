# Worker 3 — matched predicted-history exposure

Implement only src/self_audit/training/unified_trainer.py, finetune_joint.py, tests/test_candidate_c_training.py and reports/candidate_c/training_integration.md. Base HEAD 9a612485b7ead1273082d7dd52879f8e9e7a3e83. Read current HEAD/source and reports/candidate_c/architecture_contract.md (binding API/behavior/ownership). You are not alone: don't revert others; no commits/push/spawn; no core/config edits. Use coordinator ask for cross-file changes.

Implement contract minimal opt-in training.rollout.predicted_history_exposure=false / predicted_history_weight=0.1; config worker owns schema. Keep existing 130-epoch schedule, primary bootstrap/A0 objective/ConvNeXt/LRs. Opt-in auxiliary official self_audit predicted accepted-history annotation rollout during bootstrap (weight configurable, default option OFF); cold auditor evidence explicitly logged as untrained predicted, not calibrated. Auditor-only stage stays annotator frozen; when option enabled generate annotation pairs with accepted predicted history for auditor losses; no GT audit conditioning, no force acceptance, no change reject->HALT. Joint already exposes accepted predicted history; no duplicate auxiliary rollout. C solve innovation detached, primary ordinary retained graph still trains. Never count attempted rejected history as accepted history. Only latest accepted valid replay within same infer call/current model snapshot. Call invalidate_candidate_c_records after optimizer updates if available.

Log sample-level counters real_evidence_attempts, accepted_history_count, candidate_c_attempts, candidate_c_feasible, candidate_c_fallback, candidate_c_replay_failure for every stage (zeros mean measured none). Ensure logger/JSON/W&B actual fields/denominators coherent. No stale records across batches or updates. Auditor inputs always detached; auditor-only loss cannot update annotator. GT only supervised targets/loss/evaluation, no GT passed into runtime proposal. Preserve provenance/selection/calibration/best.pt/last.pt and one-config path.

Inspect core API contract, send any ambiguity to coordinator before inventing. Tests need meaningful opt-in exposure, baseline unchanged option false, parameter freezing, counters and invalidation (tiny stub/synthetic permitted, no claim trained evidence use). 1-3 focused pytest files/<2min CPU, compile/import. Available /private/tmp/self-audit-torch241/bin/python; verify. All shell commands prefix rtk. Use Semble for discovery; direct-read known files. No long training or complete suite. Report exact schedule behavior, gradient paths, tests and limitations. Follow injected Orca lifecycle exactly.
