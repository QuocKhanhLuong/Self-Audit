# Diagnostics actual-diff review — REVISE

You retain exclusive original ownership. Coordinator read the script diff and evaluation/candidate_c.py, including GT masks and geometry aggregation. No core/training/provenance edits, no commits/push, preserve others. Required fixes:

1. `_row_triplet_indices` currently excludes every rejected row and factual_support fallback. This selects only accepted successful restitution and biases correction rates. Include all eligible rows with valid historical record_turn, including factual_support and eligible ordinary fallback. Compute PROPOSAL effects using transition_candidates[current turn], then distinct RETAINED effects using candidate iff accepted else factual. Keep both denominators and acceptance/path/fallback strata explicit; don't count a rejected proposed repair as realized. Use a hand-computable fixture containing success, identity fallback and rejected harmful/beneficial proposal. Update merge_ground_truth_blocks and flat labels to distinguish proposal vs retained rather than silently changing metric meaning. Ordinary no-history rows remain unavailable. Coordinator sent this amendment before your previous settlement; it remains unimplemented in inspected file.
2. `probe_candidate_c_path` currently adds a full extra model pass even for current/feature_only/free_offsets, and treats an existing empty diagnostic key as available True. Skip baseline non-C/non-rollback mode BEFORE infer; explicit unavailable mode reason, zero actual extra forwards. Candidate C no eligible history is distinct from inactive mode. C-no-FIX and direct rollback are supported comparison paths and correctly labeled.
3. In `build_report`, default candidate_c_batches=None must inherit probe_batches (or the existing bounded caller limit), not accidentally traverse the full validation loader for programmatic callers. Document this probe is an ADDITIONAL ordinary inference pass (it does not separately re-run an individual solver, but still costs a rollout); current wording can imply no added compute. Test the batch cap and inactive-mode zero infer calls.
4. `geometry.extend(...)` retains raw GPU coordinate/attention tensors for the entire validation cohort until after loop, causing unbounded evaluation memory. Measure per batch, keep only numeric geometry summaries/rows, release raw tensors before next batch; reuse existing pure geometry functions. No image-sized tensors in aggregate report/checkpoint. Test repeated-batch outputs contain no tensors/references to originals.
5. Validate supplied geometry limits finite/nonnegative and reject negative attention probabilities rather than renormalizing them into invalid entropy; report malformed values as unavailable. Core scalar record_turn is being fixed. Remove any obsolete claim that non-geometry GT is unavailable once it lands.

Run focused diagnostics test only plus compile, no long calibration-lineage suite. Actual complete combined tests are coordinator work after integration. Deliver corrected report and exact tests; worker_done then idle.
