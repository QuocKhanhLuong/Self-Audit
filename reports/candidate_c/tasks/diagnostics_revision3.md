# Tiny diagnostics accounting correction after actual diff
Root reviewed _summarize_solver_evals and _invocation_key. Actual totals dedupe PASS, but ordinary zero-work rows are incorrectly counted as solver invocations: _invocation_key permits record_turn=None and groups ordinary turns0,2 into two phantom invocations. Your own report says3 invocations for a single turn1 solve. Fix this narrow accounting error and no other design expansion. Same ownership, no commit/push, preserve others.
- Ordinary rows with no solver origin/id and explicit all-zero evals are KNOWN no solver invocation; exclude from invocation group count while retaining row-attempt statistics. In actual 3turn ordinary->C->ordinary B2, invocations MUST1, total_forward actual<=3, raw duplicatedsum<=6. Direct rollback has zero solver invocations even with record_turn; distinguish record consumption vs solver called. Stale-record preflight with0 factual_replay/no backward/no checks is also no executed invocation (can keep attempted invocation metric separately if named).
- Partial unkeyable/missing eval rows: if they might hide solver work, exact totals must beNone even if other known groups exist; retain known subtotal. Do not report a partial sum as full known work. Explicit ordinary allzero/no solver is notunknown.
- Test mixed rows and actual 3turn output, correct report counts. No huge tests or extra framework. Focused file+compile only.
