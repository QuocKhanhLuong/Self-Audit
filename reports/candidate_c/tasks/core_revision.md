# Astra actual-code review: REVISE, blocking C1 fallback and hard solver budget
Sole root coordinator reviewed all core sources and actual diff. You retain original exclusive ownership; you are not alone, preserve other workers changes. No commit/push. Do not treat passing tests as approval when contract differs.

1. BLOCKING: _turn_candidates currently never routes C1 failure to ordinary annotation. c1_passed false/stale retained returns zero identity through factual_support; StaleReplayRecordError from expert.replay is uncaught and crashes infer. User explicitly requires replay identity failure -> disable C for that transition -> NORMAL annotator path with logged reason. Implement narrow handling for record state failure and numeric replay mismatch; do not swallow unrelated programmer exceptions. Correctly build a new ordinary replay record from fallback output and current turn if accepted; must not label as C. Mixed group C success + C1 failure must preserve exact factual batch composition for later ordinary records. Tests must prove actual infer takes current-state normal annotation on corrupted factual output/state-generation mismatch, official Auditor still controls it, no solver backward/checks for failing rows. Update report current false statement.
2. HARD runtime config validation: CandidateCConfig.__post_init__ currently allows max_backtracks>2, rho=0 and fix/regress thresholds>1 although unified_config rejects them. All entrypoints incl direct SelfAuditNet/CandidateCConfig MUST reject max_backtracks outside integers 0..2, rho<=0, probability thresholds outside[0,1]; no unbounded loop. Add direct runtime constructor tests (not just YAML).
3. Proposal geometry: current direction=-grad/maxabs has normalized-coordinate magnitude1 and clamps it against a ~.03 one-pixel box. Both alpha1 and alpha.5 saturate the same components, so the claimed full projected proposal then half-step is false and report scale claim is false. Define one proposal in feature-pixel coordinates z=(Q-P)/pixel_step: grad_z=grad_Q*pixel_step; normalized step_z=-rho*grad_z/maxabs(grad_z) per sample across all depth (or explicit lr in documented pixel-gradient units). step_Q=step_z*pixel_step; project once; then check projected Q1 and P+0.5*(Q1-P), preserving bounds/domain. No extra gradients. Test actual non-square feature grid verifies largest step<=rho and half-check really halves realized displacement, including saturation at domain boundary. Keep meaningful success fixture (controlled small rho okay) and default-budget tests.
4. Solver eligible must explicitly intersect record.accepted, not rely on retained!=factual to exclude rejected rows. They remain in forward for batch identity only.
5. Per-row diagnostics must distinguish feasible from improved. Currently both are settled; feasible-but-not-improved is falsely labelled infeasible. Record actual checked objective and constraint violation for failed proposals where measured, plus final selected factual result separately if needed. A failed feasibility check must expose violation, not only null. No-proposal -> null, identity fallback displacement/innovation zero. Keep existing field contracts with additions and inform diagnostics consumer via report/message.
6. validate_coordinate_override must check full device equality, not only device.type (cuda:0 vs cuda:1). Clean unused imports if safe. Do not change QKV semantics, A0, parameters/buffers, officialgate or 130epochrecipe.

Focused tests/test_candidate_c.py and compile only, under OMP/MKL threads1; do not rerun broad existing suites. Report exact behavioral changes and honest test evidence, settle when complete. Runtime report stream and root final integration will retest quiescent tree.
